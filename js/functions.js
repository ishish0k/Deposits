//functions
// Ismael Iribe

var pi = 3.14159;
var radius = 5;

var getArea = function (r) {
    var area = r * r * pi;
    console.log(name + "'s circle has" + area + " area, and " +message);
};

getArea(2);
getArea(5);
getArea(10);